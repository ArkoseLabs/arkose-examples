const React = require('react');
const {View} = require('react-native');
const WebView = props => React.createElement(View, props);
module.exports = {WebView, WebViewMessageEvent: undefined};
module.exports.default = WebView;
